server:
  port: 8081

spring:
  application:
    name: T1-DAW-II-Consumer-Ninfa

  datasource:
    url: jdbc:mysql://localhost:3306/DAWIItest
    username: root
    password: Stigmata2612.
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

  kafka:
    consumer:
      bootstrap-servers: localhost:9092
      group-id: grupo-dragonball
      auto-offset-reset: earliest
